[package]
name = "fast-zip"
version = "0.1.0"
edition = "2024"

exclude = ["example"]

[workspace]
members = ["example", "."]

[features]
default = []
all = ["fast-stream/all"]

[dependencies]
thiserror = "2.0"
indexmap = "2.8.0"
fast-stream = { path = "../fast-stream" }